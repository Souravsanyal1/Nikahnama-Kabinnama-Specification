rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Security Rules for Nikahnama Central Database
    // Allows public read and write access during development.
    // In production, you can restrict writes to authenticated registrars only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
